{% extends 'main/base-front.html.twig' %}

{% block title %}nos stations{% endblock %}

 {% block nav %}<ul class="nav">
                        <li><a href="{{ path('app_accueil') }}" >Accueil </a></li>
                        <li><a href="{{ path('app_velo') }}" class="active">Nos velos</a></li>
                        <li><a href="{{ path('app_station') }}">Nos stations</a></li>
                        <li><a href="{{ path('app_evenement') }}">Evenements</a></li>
                        <li><a href="{{ path('app_reclamation') }}" >Reclamation</a></li>
                        <li><details ><summary><a >Profile <img src="front-office/assets/images/profile-header.jpg" alt=""></a></summary>
                        <a  href="{{ path('app_profile') }}" class="link-unstyled"  >mon profile</a><a href="{{ path('app_login') }}" >deconnexion</a> 
                        </details>
                        </li>
                    </ul>{% endblock %}

{% block body %}<h1> Liste des Velos </h1>

    <table class="table">
        <tbody>
            <tr>
                <th>IdVelo</th>
                <td>{{ velo.idVelo }}</td>
            </tr>
            <tr>
                <th>Titre</th>
                <td>{{ velo.titre }}</td>
            </tr>
            <tr>
                <th>Prix</th>
                <td>{{ velo.prix }}</td>
            </tr>
            <tr>
                <th>Image</th>
                <td>
                    {% if velo.image %}
                        <img src="{{ asset('uploads/velo/images/'~velo.image) }}" alt="{{ velo.titre }}">
                    {% else %}
                        No image
                    {% endif %}
                </td>
            </tr>
            <tr>
                <th>Qte</th>
                <td>{{ velo.qte }}</td>
            </tr>
            
        </tbody>
    </table>

    <a href="{{ path('app_velo_index') }}">back to list</a>

    <a href="{{ path('app_velo_edit', {'idVelo': velo.idVelo}) }}">edit</a>

    {{ include('velo/_delete_form.html.twig') }}
{% endblock %}

