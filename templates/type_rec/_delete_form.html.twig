<form method="post" action="{{ path('app_type_rec_delete', {'id': type_rec.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ type_rec.id) }}">
    <button class="btn">Delete</button>
</form>
