{% extends 'base.html.twig' %}
{% block title %}ajouter reservation{% endblock %}
{% block content %}
    <h1>New Reservation</h1>

    {{ form_start(form) }}
        {{ form_widget(form) }}
        <button type="submit">Save</button>
    {{ form_end(form) }}
{% endblock %}