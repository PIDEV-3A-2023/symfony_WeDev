{% extends 'base.html.twig' %}

{% block title %}Velo{% endblock %}

{% block body %}
    <h1>Velo</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>IdVelo</th>
                <td>{{ velo.idVelo }}</td>
            </tr>
            <tr>
                <th>Titre</th>
                <td>{{ velo.titre }}</td>
            </tr>
            <tr>
                <th>Prix</th>
                <td>{{ velo.prix }}</td>
            </tr>
            <tr>
                <th>Image</th>
                <td>{{ velo.image }}</td>
            </tr>
            <tr>
                <th>Qte</th>
                <td>{{ velo.qte }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_sahar_index') }}">back to list</a>

    <a href="{{ path('app_sahar_edit', {'idVelo': velo.idVelo}) }}">edit</a>

    {{ include('sahar/_delete_form.html.twig') }}
{% endblock %}
