<!DOCTYPE html>

<title>Edit ReservationVelo</title>

{% block body %}
    <h1>Edit ReservationVelo</h1>

    {{ include('reservation_velo/_form.html.twig', {'button_label': 'Update'}) }}

    <a href="{{ path('app_reservation_velo_index') }}">back to list</a>

    {{ include('reservation_velo/_delete_form.html.twig') }}
{% endblock %}
