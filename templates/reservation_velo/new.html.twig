<!DOCTYPE html>

<title>New ReservationVelo</title>

{% block body %}
    <h1>Create new ReservationVelo</h1>

    {{ include('reservation_velo/_form.html.twig') }}

    <a href="{{ path('app_reservation_velo_index') }}">back to list</a>
{% endblock %}
