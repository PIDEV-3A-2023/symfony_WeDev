{% extends 'main/base-back.html.twig' %}

{% block title %}User index{% endblock %}

{% block body %}<div class="container">
    <h1>User index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Iduser</th>
                <th>Nomuser</th>
                <th>Prenomuser</th>
                <th>Datenaiss</th>
                <th>Numtel</th>
                <th>Email</th>
                <th>Adresse</th>
                <th>Imguser</th>
                <th>Mdp</th>
                <th>Etatcompte</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for user in users %}
            <tr>
                <td>{{ user.iduser }}</td>
                <td>{{ user.nomuser }}</td>
                <td>{{ user.prenomuser }}</td>
                <td>{{ user.datenaiss ? user.datenaiss|date('Y-m-d') : '' }}</td>
                <td>{{ user.numtel }}</td>
                <td>{{ user.email }}</td>
                <td>{{ user.adresse }}</td>
                <td><img src="{{ asset('tsawir/velo/' ~  user.imguser) }}" alt="{{ user.nomuser }}" style="max-width: 100%; "></td>
                <td>$2y$12$Y9nRkFvoR3D</td>
                <td>{{ user.etatcompte }}</td>
                <td>
                    <a href="{{ path('app_aziz_show', {'iduser': user.iduser}) }}">show</a>
                    <a href="{{ path('app_aziz_edit', {'iduser': user.iduser}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="11">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('app_aziz_new') }}">Create new</a>
{% endblock %}
