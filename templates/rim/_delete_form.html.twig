<form method="post" action="{{ path('app_rim_delete', {'idType': type_rec.idType}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ type_rec.idType) }}">
    <button class="btn">Delete</button>
</form>
