{% extends 'base.html.twig' %}

{% block title %}Edit Reserv{% endblock %}

{% block body %}
    <h1>Edit Reserv</h1>

    {{ include('reserv/_form.html.twig', {'button_label': 'Update'}) }}

    <a href="{{ path('app_reserv_index') }}">back to list</a>

    {{ include('reserv/_delete_form.html.twig') }}
{% endblock %}
